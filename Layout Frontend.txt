📂 Estrutura do Frontend
1. Módulo de Autenticação
Responsável por: Registro e Login de usuários.

Funcionalidades:

Formulário de login (username + senha).

Formulário de registro (username, senha, confirmação, papel).

Validação de entrada (mínimo de caracteres, senhas iguais).

Exibição de mensagens de erro e sucesso.

Lógica:

Ao logar, salva o token JWT no localStorage ou sessionStorage.

Redireciona para o Dashboard após login.

Controle de acesso: se role = admin, habilita recursos extras.

2. Dashboard Principal
Responsável por: Ser a página inicial após login.

Funcionalidades:

Exibir pastas principais (Financeiro, RH, Projetos).

Mostrar arquivos recentes ou últimas ações.

Botão para reset demo (apenas admin).

Lógica:

Consome /folders para listar pastas.

Exibe atalhos para cada módulo (Pastas, Arquivos, Subpastas).

Mostra status do sistema via /health.

3. Gerenciamento de Pastas
Responsável por: CRUD de pastas.

Funcionalidades:

Listar todas as pastas.

Criar nova pasta (admin).

Excluir pasta (admin, não padrão).

Visualizar detalhes de uma pasta.

Lógica:

Usa /folders para listar.

Usa /folders/:name para detalhes.

Usa POST /folders e DELETE /folders/:name para criar/excluir.

Controle de acesso: botões de criar/excluir só aparecem para admin.

4. Gerenciamento de Arquivos
Responsável por: CRUD de arquivos dentro de pastas.

Funcionalidades:

Listar arquivos de uma pasta (com ordenação por nome/data).

Abrir arquivo e visualizar conteúdo.

Criar novo arquivo (admin).

Editar arquivo (admin, não padrão).

Excluir arquivo (admin, não padrão).

Lógica:

Usa /files/:folderName para listar.

Usa /files/:folderName/:fileName para abrir.

Usa POST, PUT, DELETE para criar/editar/excluir.

Ordenação via query ?sort=name ou ?sort=date.

5. Gerenciamento de Subpastas
Responsável por: CRUD de subpastas dentro de uma pasta.

Funcionalidades:

Listar subpastas de uma pasta.

Criar nova subpasta (admin).

Excluir subpasta (admin, não padrão).

Visualizar detalhes de uma subpasta.

Lógica:

Usa /subfolders/:folderName para listar/criar.

Usa /subfolders/:folderName/:subfolderName para detalhes/excluir.

Controle de acesso: botões de criar/excluir só aparecem para admin.

6. Busca Global
Responsável por: Pesquisar arquivos e pastas.

Funcionalidades:

Campo de busca no topo (navbar).

Pesquisa contextual: dentro da pasta atual e subpastas.

Ordenação dos resultados.

Lógica:

Usa endpoint de busca (já implementado no backend).

Exibe resultados em lista com link para abrir arquivo/pasta.

7. Admin Tools
Responsável por: Recursos exclusivos de administradores.

Funcionalidades:

Reset demo (POST /reset).

Criar/excluir pastas e subpastas.

Criar/editar/excluir arquivos não padrão.

Lógica:

Botões e menus só aparecem se role = admin.

Usa token JWT para validar permissões.

8. Navbar / Layout Global
Responsável por: Navegação e identidade visual.

Funcionalidades:

Links para Dashboard, Pastas, Arquivos, Subpastas, Busca.

Exibir usuário logado e papel (admin/user).

Botão de logout (remove token e volta para login).

Lógica:

Estado global de autenticação.

Condicional para mostrar menus extras de admin.